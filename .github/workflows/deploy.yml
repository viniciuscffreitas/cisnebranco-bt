name: Deploy

on:
  push:
    branches: [main]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy notification
        run: echo "Push to main detected. VPS auto-deploy will pick up changes within 2 minutes."
